module.exports = {
  // Print width (characters per line)
  printWidth: 100,

  // Number of spaces per indentation level
  tabWidth: 2,

  // Use spaces instead of tabs
  useTabs: false,

  // Semicolons at the ends of statements
  semi: true,

  // Use single quotes instead of double quotes
  singleQuote: true,

  // Quote props only when needed
  quoteProps: 'as-needed',

  // Use single quotes in JSX
  jsxSingleQuote: false,

  // Add trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'es5',

  // Spaces between brackets in object literals
  bracketSpacing: true,

  // Put the > of a multi-line JSX element at the end of the last line
  jsxBracketSameLine: false,

  // Include parentheses around arrow function parameters when needed
  arrowParens: 'avoid',

  // Range of code to format
  rangeStart: 0,
  rangeEnd: Infinity,

  // Parser to use
  // Prettier will automatically infer based on file extension
  // parser: undefined,

  // Specify file path to consider the file location
  // filepath: undefined,

  // Require pragmas at top of file to format
  requirePragma: false,

  // Insert pragmas at top of formatted files
  insertPragma: false,

  // Wrap prose (markdown)
  proseWrap: 'preserve',

  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',

  // Enforce LF line endings
  endOfLine: 'lf',

  // Enable embedded language formatting
  embeddedLanguageFormatting: 'auto',
};
